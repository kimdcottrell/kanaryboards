{
  "enabledMcpjsonServers": [
    "Astro docs",
    "Daisy UI docs",
    "Deno docs",
    "Docker docs",
    "Google GenAI docs",
    "Zod docs",
    "React docs",
    "Preact docs",
    "Tailwind docs"
  ],
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "deno eval 'const s=JSON.parse(await Deno.readTextFile(Deno.env.get(\"PWD\")+\"/.claude/settings.json\").catch(()=>\"{}\")); const i=JSON.parse(await Deno.readTextFile(Deno.env.get(\"HOME\")+\"/.claude/plugins/installed_plugins.json\").catch(()=>\"{}\")); const m=Object.keys(s.enabledPlugins??{}).filter(p=>!(i.plugins??{})[p]); if(m.length)console.log(JSON.stringify({systemMessage:\"Plugin warning: enabled but not installed: \"+m.join(\", \")}));' 2>/dev/null || true",
            "statusMessage": "Checking plugins..."
          },
          {
            "type": "command",
            "command": "jq 'if has(\"hasCompletedOnboarding\") then . else . + {\"hasCompletedOnboarding\": true} end' ~/.claude.json > /tmp/.claude.json.tmp && mv /tmp/.claude.json.tmp ~/.claude.json"
          }
        ]
      }
    ]
  },
  "enabledPlugins": {
    "typescript-lsp@claude-plugins-official": true,
    "context7@claude-plugins-official": true
  }
}
