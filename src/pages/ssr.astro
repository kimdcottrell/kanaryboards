---
import TestingIsland from "@components/TestingIsland.astro";
const import_secret = import.meta.env.PUBLIC_CLERK_PUBLISHABLE_KEY;
const deno_secret = Deno.env.get("PUBLIC_CLERK_PUBLISHABLE_KEY");
---

<h1>SSR</h1>

<p>Import: {import_secret}</p>

<p>Deno: {deno_secret}</p>

<TestingIsland server:defer />

Astro.locals.runtime.env
