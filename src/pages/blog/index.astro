const entry = await getEntry("blog", slug);

console.log("entry")
// example of bubbling errors thru request chain
if (!entry) {
    Astro.response.status = 405;
    Astro.response.statusText = "This article does not exist.";
}